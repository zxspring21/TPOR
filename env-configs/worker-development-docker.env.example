# Worker Development Environment (With Docker)
# This environment is for development with Docker containers

# JWT Configuration
JWT_SECRET=dev-docker-super-secret-jwt-key-that-is-at-least-32-characters-long

# Authentication
AUTH_USERNAME=admin
AUTH_PASSWORD=dev-docker-password-123

# File Storage
LOCAL_STORAGE_PATH=uploads/docker

# Environment
ASPNETCORE_ENVIRONMENT=Development

# Database (Connected to Docker MySQL)
DATABASE_CONNECTION_STRING=Server=mysql;Port=3306;Database=TPOR_Dev;Uid=root;Pwd=docker-password-123;

# Message Queue (Mock implementation for development)
# MESSAGE_QUEUE_TYPE=mock

# Google Cloud (Not used in development)
# GOOGLE_CLOUD_PROJECT_ID=
# GOOGLE_CLOUD_CREDENTIALS_PATH=
